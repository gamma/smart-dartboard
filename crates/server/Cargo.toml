[package]
name = "sdb-server"
description = "Headless HTTP and WebSocket host for the Smart Dartboard runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
axum = { version = "0.8", features = ["ws"] }
sdb-board = { path = "../board" }
sdb-companion = { path = "../companion" }
sdb-contracts = { path = "../contracts" }
sdb-diagnostics = { path = "../diagnostics" }
sdb-game-core = { path = "../game-core" }
sdb-runtime = { path = "../runtime" }
sdb-storage = { path = "../storage" }
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tower-http = { version = "0.6", features = ["fs", "set-header"] }
uuid = { version = "1.18", features = ["v4"] }

[dev-dependencies]
futures-util = "0.3"
tokio-tungstenite = "0.29"
tower = { version = "0.5", features = ["util"] }

[lints]
workspace = true
